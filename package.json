{
  "name": "json-store",
  "version": "1.0.0",
  "description": "Simple json store",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx nodemon . --ignore 'data/'",
    "start": "NODE_ENV=production node .",
    "tdd": "jest --watch --config jest.unit.config.js",
    "test": "jest --config jest.unit.config.js",
    "integ": "rm -f data/test/post.json data/test/add.json data/test/del.json && jest --config jest.integ.config.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
