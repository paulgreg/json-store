{
  "name": "json-store",
  "version": "1.0.0",
  "description": "Simple json store",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx nodemon .",
    "start": "NODE_ENV=production node .",
    "tdd": "jest --watch --config jest.unit.config.js",
    "test": "jest --config jest.unit.config.js",
    "integ": "rm -f data/test/post.json && jest --config jest.integ.config.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^27.3.1",
    "supertest": "^6.1.6"
  }
}
